on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Run DEFUSE's conditional model for Ansible
    steps:
      - name: Defect Prediction
        id: hello
        uses: stefanodallapalma/my-github-action@v26
        with:
          model: '24242603'
          language: 'ansible'