on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: stefanodallapalma/my-github-action@v24
        with:
          model: 'model1234'
          language: 'ansible'